name: display soft version

on: push

jobs:
  version:
    runs-on: ubuntu-latest
    steps:
      - run : echo 'Hello World'
      - name: show python version
        run: python -V
        shell: python
